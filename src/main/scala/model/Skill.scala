package model

object Skill extends Enumeration {
  type Skill = Value
  val
    NAME,
      ANN, EVE, CAROLINE,
      ADAM, MARTIN, MACIEJ,
    NOTHING, DATE, DEMO, ECHO, QUIT, TASK, TIME, TIME2, VERSION, WEATHER = Value
}
